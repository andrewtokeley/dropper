# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'dropper' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for dropper
  pod 'PureLayout'
  pod 'Viperit'
  
  target 'dropperTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
